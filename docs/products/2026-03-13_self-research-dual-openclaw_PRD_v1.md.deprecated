# 产品需求文档 - 纯自研双 OpenClaw 协作系统

**文档版本**：v1.0  
**创建日期**：2026-03-13  
**创建者**：小米辣（PM 代理）  
**状态**：待开发  
**Issue**：待创建  
**开发者**：小米粒（Dev 代理）  
**优先级**：P0（最高）

---

## 官家指令

**优先级顺序**：
1. **P0**：纯自研方案的双 OpenClaw 协作（先基础功能，后迭代）
2. **P1**：双米粒协作系统（MemOS 方案）
3. **P2**：Issue #7（AI 确定性控制工具发布）

**策略**：先实现基础功能，后期再迭代

---

## 1. 需求概述

### 1.1 背景

**核心需求**：纯自研双 OpenClaw 协作系统，不依赖第三方云服务。

**目标**：
- ✅ 完全自主可控
- ✅ 可定制扩展
- ✅ 无第三方依赖
- ✅ 先实现基础功能（MVP），之后慢慢拓展

### 1.2 范围

**MVP 包含**：
- 共享存储（SQLite/文件系统）
- 消息路由（Flask 轻量级服务）
- 上下文注入（启动时加载）
- 简单 API（读取/写入记忆）

**MVP 不包含**（后续迭代）：
- 复杂权限控制
- 分布式部署
- 高级检索功能
- 监控告警

---

## 2. 系统架构

### 2.1 整体架构

```
OpenClaw A (小米辣-PM)  OpenClaw B (小米粒-Dev)
      Port 3000              Port 3001
            │                      │
            └──────────┬───────────┘
                       │
                ┌──────▼──────┐
                │ 中继服务    │
                │ (Flask)     │
                │ Port 5000   │
                └──────┬──────┘
                       │
                ┌──────▼──────┐
                │ SQLite      │
                │ (共享存储)  │
                └─────────────┘
```

### 2.2 核心组件

#### 组件 1：中继服务（Relay Service）
- **技术栈**：Python 3.8+ + Flask
- **职责**：
  - 接收 OpenClaw 的消息
  - 路由到对应智能体
  - 写入 SQLite 数据库
  - 提供记忆检索 API

#### 组件 2：共享存储（SQLite）
- **表结构**：
  ```sql
  CREATE TABLE messages (
      id INTEGER PRIMARY KEY,
      agent_id TEXT,  -- 'xiaomila_pm' or 'xiaomili_dev'
      content TEXT,
      timestamp DATETIME,
      context_tags TEXT
  );
  ```

#### 组件 3：OpenClaw 插件（可选）
- **职责**：自动调用中继服务 API
- **实现**：简单的 Python 脚本或 OpenClaw skill

---

## 3. 功能需求

### 3.1 MVP 功能（P0）

#### 功能 1：消息写入 ⭐⭐⭐⭐⭐
- **描述**：智能体的产出自动写入中继服务
- **API**：`POST /api/messages`
- **输入**：`{agent_id, content, context_tags}`
- **输出**：`{success, message_id}`
- **验收标准**：
  - [ ] 写入成功率 > 99%
  - [ ] 响应时间 < 100ms

#### 功能 2：记忆检索 ⭐⭐⭐⭐⭐
- **描述**：智能体启动后检索相关上下文
- **API**：`GET /api/messages?agent_id=xxx&limit=10`
- **输出**：消息列表
- **验收标准**：
  - [ ] 检索准确率 > 90%
  - [ ] 响应时间 < 500ms

#### 功能 3：上下文注入 ⭐⭐⭐⭐⭐
- **描述**：启动时自动加载共享上下文
- **实现**：OpenClaw 启动脚本调用中继服务 API
- **验收标准**：
  - [ ] 自动加载最近 10 条消息
  - [ ] 支持手动指定加载数量

#### 功能 4：简单管理界面 ⭐⭐⭐
- **描述**：查看消息历史
- **实现**：Flask 简单 HTML 页面
- **验收标准**：
  - [ ] 可查看最近消息
  - [ ] 可按 agent_id 过滤

### 3.2 后续迭代（P1/P2）

#### P1 功能
- 权限控制（只读/读写）
- 消息分类和标签
- 高级检索（关键词/时间范围）

#### P2 功能
- 分布式部署
- 监控告警
- 性能优化

---

## 4. 技术需求

### 4.1 技术栈
- **语言**：Python 3.8+
- **Web 框架**：Flask 2.0+
- **数据库**：SQLite 3（内置）
- **部署**：单机部署

### 4.2 性能要求
- **写入延迟**：< 100ms
- **检索延迟**：< 500ms
- **并发支持**：> 10 QPS

### 4.3 安全要求
- **API 认证**：简单 token 认证
- **数据备份**：定期备份 SQLite 文件

---

## 5. 开发计划

### 阶段 1：技术设计（预计 2 小时）
- [ ] 详细架构设计
- [ ] API 接口定义
- [ ] 数据库设计
- [ ] 创建 GitHub Issue

### 阶段 2：核心开发（预计 4 小时）
- [ ] Flask 中继服务
- [ ] SQLite 数据库
- [ ] 消息写入 API
- [ ] 记忆检索 API

### 阶段 3：集成测试（预计 2 小时）
- [ ] OpenClaw 集成
- [ ] 上下文注入测试
- [ ] 完整流程测试

### 阶段 4：文档和验收（预计 1 小时）
- [ ] 使用文档
- [ ] API 文档
- [ ] 验收测试

**总计**：约 9 小时（1 天 + 1 小时）

---

## 6. 智能体角色

### 小米辣（PM 代理）
- **职责**：需求分析、PRD 创建、Review 验收
- **技能**：requirement-analysis, prd-creator, reviewer
- **默认策略**：收到需求→创建 PRD→Review→批准

### 小米粒（Dev 代理）
- **职责**：技术设计、开发实现、测试部署
- **技能**：tech-designer, developer, tester, devops
- **默认策略**：收到 PRD→设计→开发→测试→部署

---

## 7. 协作流程

```
1. 官家提出需求
   ↓
2. 小米辣创建 PRD（本文档）
   ↓
3. 小米粒技术设计（2 小时）
   ↓
4. 小米粒开发 MVP（4 小时）
   ↓
5. 小米辣 Review（1 小时）
   ↓
6. 小米粒修复和部署（1 小时）
   ↓
7. 验收完成
```

---

## 8. 验收标准

### 8.1 功能验收
- [ ] 中继服务正常运行（Port 5000）
- [ ] 消息写入 API 正常
- [ ] 记忆检索 API 正常
- [ ] 两个 OpenClaw 实例能正常读写记忆

### 8.2 性能验收
- [ ] 写入延迟 < 100ms
- [ ] 检索延迟 < 500ms
- [ ] 并发支持 > 10 QPS

### 8.3 协作验收
- [ ] 完成一次完整协作流程
- [ ] 小米辣能读到小米粒的产出
- [ ] 小米粒能读到小米辣的产出

---

## 9. 下一步行动

### 立即执行（小米粒）
1. ⏳ **技术设计**（2 小时）
   - 详细架构设计
   - API 接口定义
   - 数据库设计
   - 创建 GitHub Issue

2. ⏳ **核心开发**（4 小时）
   - Flask 中继服务
   - SQLite 数据库
   - 消息写入/检索 API

3. ⏳ **集成测试**（2 小时）
   - OpenClaw 集成
   - 完整流程测试

4. ⏳ **文档和验收**（1 小时）
   - 使用文档
   - API 文档
   - 验收测试

**预计完成时间**：今天内（约 9 小时）

---

## 10. 参考资源

- **Flask 文档**：https://flask.palletsprojects.com/
- **SQLite 文档**：https://www.sqlite.org/docs.html
- **OpenClaw 文档**：https://docs.openclaw.ai

---

**PRD 版本**：v1.0  
**创建时间**：2026-03-13 13:05  
**创建者**：小米辣（PM 代理）  
**开发者**：小米粒（Dev 代理）  
**状态**：待开发

---

*请 @小米粒 立即开始技术设计！*

**官家指令**：优先级最高 - 纯自研方案的双 OpenClaw 协作（先基础功能，后迭代）
